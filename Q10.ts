// Program: Friends' Names

// Store the names of friends in an array
var names = ["MOIN", "SARMAD", "REHAN", "Daniyal",];

// Print each person's name using a loop
console.log("List of Friends' Names:");
for (var i = 0; i < names.length; i++) {
    console.log(names[i]);
}
